// Root build file — plugin versions only.
// Actual module configuration lives in each module's build.gradle.kts.
plugins {
    // versions are applied via plugins block in subprojects; root declares nothing to enforce.
}
